spring:
  datasource:
    url: jdbc:postgresql://localhost:2345/db
    driver-class-name: org.postgresql.Driver
    username: supplier
    password: supplier
  jpa:
    hibernate:
      ddl-auto: create-drop

